# @akin2unde/flowui-angular

Tailwind-styled standalone FlowUI components for Angular. FlowUI ships compiled
CSS, so the host application does not need Tailwind.

```bash
npm install @akin2unde/flowui-angular @fortawesome/fontawesome-free
```

Add `@akin2unde/flowui-angular/styles.css` to the application's styles, then
import only the standalone `fui-*` components it uses.

See the [FlowUI repository](https://github.com/akin2unde/FlowUI) for the full
component gallery and API documentation.
